From:
To:
Date:
Subject: XML
text/xml | type code = | uniform type = public.xml | conforms to = public.text | magic = | owner = World Wide Web Consortium | genre = Markup language | container for = | contained by = | extended from = Standard Generalized Markup Language | extended to = List of XML markup languages, including:XHTML, RSS, Atom (standard), KML | standard = [http://www.w3.org/TR/2008/REC-xml-20081126/ 1.0 (Fifth Edition)] [http://www.w3.org/TR/2006/REC-xml11-20060816/ 1.1 (Second Edition)] | free = Yes }} The '''Extensible Markup Language''' (short XML) is a markup language like HTML but is extensible. Data Basecamp|url=https://databasecamp.de/en/data/xml-file|access-date=2022-09-25|language=en-US}} It's created by the World Wide Web Consortium (W3C). XML defines rules for the construction of a document. XML adds context to the information in a document. It does not say how this should be displayed. Some programs get information out of an XML-document. To do that, they need an API. Data Basecamp|url=https://databasecamp.de/en/data/api-en|access-date=2022-08-11|language=en-US}} There are many APIs for XML.JSON was built to replace this purpose of XML. You can write a description of an XML document in a way that is useful for programmers. There are several languages for this; the best known is called DTD. == Syntax == XML uses less than () to show tags. For example, a paragraph in HTML would be p. A closing tag is a tag used to enclose the value of the tag. The tag has a slash (/) before its name. For example, /p A tag which is empty can be represented as an opening tag but with a slash before the . For example, p/ (or p /) The XML specification defines a XML validation as a well-formed XML document which also conforms to the rules of a Document Type Definition (DTD). == Languages == The following languages are based on XML. === Text === * XSL-FO * DocBook * DITA * XHTML (HTML based on ALL) * Text Encoding Initiative (Text Encoding Initiative) * NITF (XML) (News Industry Text Format) === Images === * Scalable Vector Graphics (vector graphics) * X3D (3D modelling language) * Collaborative Design Activity (Language to change informations between different 3D programs) === Earth === * Geography Markup Language (GML) * GPS Exchange Format (GPX): XML for Global Positioning System informatons * Keyhole Markup Language (KML): coordinates for Google Earth * OpenImmo : XML for Houses with position informations === Multimedia === * SMIL (Informations with times from multimedia) * MPEG-7 (for MPEG-7) * Laszlo (Auszeichnungssprache) (LZX) === Safety === * SAML (describe and send informations about safety) * XML Signature * XML Encryption === Other === There are a lot more languages that use XML. A couple of them are: * RSS * WAP/WML ==Sources== == Other websites == * [http://www.xeml.net XeML.net] * [http://www.w3.org/XML/ W3C XML Page] Category:Markup languages
